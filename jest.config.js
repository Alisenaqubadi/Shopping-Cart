// Deprecated Jest config: project migrated to Vitest.
// Kept for reference. Use `vitest` and `vitest.config.js` instead.
export default {
  // This file is intentionally left as a reference for the previous Jest setup.
}
